name: Fiscal Engine CI

on:
  push:
    branches: [ main, build, feat/orchestrator-integration, debug, ci/github-actions ]
  pull_request:
    branches: [ main ]

jobs:
  test-and-coverage:
    name: Run tests and collect coverage
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2 

      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: 'pip'

      - name: Install Package & dependencies
        run: |
          python -m pip install --upgrade pip
          pip install --ignore-installed --no-cache-dir -e ".[test,api,observability]" 

      - name: Run Tier 1 Tests (Unit Tests)
        run: pytest -m "not performance and not contract and not slow" --cov=robot --cov-report=xml

      - name: Upload results to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: true 