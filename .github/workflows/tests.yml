name: Fiscal Engine CI & Coverage

on:
  push:
    branches: [ main, debug, build, feat/orchestrator-integration, ci/github-actions-setup, ci/github-actions ]
  pull_request:
    branches: [ main ]

jobs:
  test-and-coverage:
    name: Run tests and collect coverage
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2 

      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: 'pip'

      - name: Install Package & Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[test,observability,api]" 

      - name: Run pytest

        run: pytest -m "not slow" --cov=robot --cov-report=xml

      - name: Upload results to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: true 